# Testdaten — Import-Flow

Ablageort für JSON-Testdateien für den Routinen-Import.

Erwartete Datei für den ersten Test:
- `gig_routine_import.json` — liefert der User separat

Verwendung: In der App unter "Meine Routinen" auf **Importieren** klicken,
Datei aus diesem Ordner auswählen, Vorschau prüfen, Import bestätigen.
